// Les réglages sont volontairement strictement identiques à ceux de
// « Segmenter les mots » : même banque, même sélection et même quantité.
export {
  getDefaultSettings,
  renderToolSettings,
  readToolSettings
} from "../segmenter-mots/config.js";
